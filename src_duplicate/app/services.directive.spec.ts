import { ServicesDirective } from './services.directive';

describe('ServicesDirective', () => {
  it('should create an instance', () => {
    const directive = new ServicesDirective();
    expect(directive).toBeTruthy();
  });
});
